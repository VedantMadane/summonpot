Validate capability contracts when an endpoint is declared: bindings must name real arguments and request fields, an operation that declares `bind` must bind every argument, `FromResult` must name a declared operation with a validatable output field, and the operations must be orderable.
