Replace the `Pot` application object and `@pot.summon(...)` syntax with `Summon`, a module-level `summon` variable, and direct `@summon(...)`; `Summon.summon(...)` remains a temporary source-compatibility alias.
